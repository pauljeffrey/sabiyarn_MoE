# Training on a bare GPU box (vast.ai): pip install -r requirements-train.txt
#
# torch is NOT listed: use a CUDA PyTorch image (torch >= 2.8) so you don't re-download or
# mismatch the CUDA build. Do not use requirements.txt here -- it pins transformers 4.55 and
# huggingface_hub 0.34, but the checkpoints were saved by transformers 5.14.1 (see
# config.json's transformers_version) and the model code (trust_remote_code modeling.py) targets
# the 5.x API; transformers 5 also needs a newer huggingface_hub than 0.34.
transformers==5.14.1
accelerate>=1.10.0
safetensors
numpy
omegaconf
PyYAML
python-dotenv
structlog
boto3
mlflow>=2.16
wandb
psutil
tqdm
